apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: timetable-service
  labels:
    app: timetable-service
  annotations:
    traefik.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: traefik
  rules:
    - host: sau-portal.de
      http:
        paths:
          - path: /timetable # specify under which path your service should be reachable
            pathType: Prefix
            backend:
              service:
                name: timetable-service
                port:
                  number: 3000